---
title:
layout: default.liquid
---

<header class="py-5 bg-light text-center">
	<div class="container">
		<h1 class="display-4">{{ title }}</h1>
		<!-- <p class="lead">Speaker</p> -->
	</div>
</header>

<section class="container py-6 py-md-8">
	{{ content }}
</section>

<section class="container py-6 py-md-8">
	{% assign talks = collections.talks %}
	{% for talk in talks %}
		{{ talk.title }}
	{% endfor %}
</section>