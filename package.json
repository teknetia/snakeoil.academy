{
  "name": "snakeoil.academy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:eleventy": "npx @11ty/eleventy --serve --watch",
    "watch:sass": "npm run sass -- --watch",
    "start": "npx npm-run-all sass --parallel watch:*",
    "build": "npx @11ty/eleventy && npm run sass",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --style=compressed ./src/scss/style.scss ./src/_includes/css/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "bootstrap": "^4.5.2",
    "liquidjs": "^9.15.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.10"
  },
  "dependencies": {}
}
